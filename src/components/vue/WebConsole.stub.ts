/**
 * Vue WebConsole Component Stub
 * This is a build-time stub to avoid Vue dependency issues
 */

// Stub component for Vue
export const WebConsole = {
  name: 'WebConsole',
  template: '<div>Vue WebConsole requires Vue.js to be installed</div>',
};

export default WebConsole;

// Runtime warning for missing Vue dependency
if (typeof window !== 'undefined') {
  console.warn('Vue WebConsole component requires vue to be installed');
}
