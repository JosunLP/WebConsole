/**
 * Angular WebConsole Component Stub
 * This is a build-time stub to avoid Angular dependency issues
 */

// Stub interface for Angular component
export interface WebConsoleComponent {
  // Angular component interface stub
}

// Stub interface for Angular module
export interface WebConsoleModule {
  // Angular module interface stub
}

// Runtime warning for missing Angular dependency
if (typeof window !== 'undefined') {
  console.warn(
    'Angular WebConsole component requires @angular/core to be installed'
  );
}
